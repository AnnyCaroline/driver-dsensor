#include "dsensor.ceu"

_Serial.begin(9600);
_Serial.println("init");

var& DSensor mysensor = spawn DSensor(_PCINT4, 7, _); //12

loop do
    var bool value = await mysensor.change;
    _Serial.println("teste");
end